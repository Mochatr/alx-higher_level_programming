#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    divList = []

    for n in my_list:
        # Check if number is multiple by 2
        number = n % 2 == 0
        result.append(number)

    return result
